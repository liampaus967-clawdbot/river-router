# River Router Data Pipeline Configuration

# AWS S3 (for percentile stats and live conditions)
S3_BUCKET=river-router-data
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=...  # Or use IAM role
# AWS_SECRET_ACCESS_KEY=...

# PostgreSQL (for gauge locations only)
DATABASE_URL=postgresql://user:pass@host:5432/river_router

# Processing
MAX_WORKERS=10
